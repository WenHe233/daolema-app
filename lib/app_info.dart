/// 应用元信息。
///
/// 显示用版本号——所有展示版本号的地方（设置页脚、关于页）都引用此变量。
/// 改版本时请同步更新 `pubspec.yaml` 的 `version:`（其去掉前缀 `v` 后应与此一致）。
const String kAppVersion = 'v1.0.0';

/// 项目仓库地址。
const String kProjectUrl = 'https://github.com/WenHe233/daolema-app';
